import React, { useState, useEffect } from "react";

function Item (props){
    const th = {
        width: '100px'
    };
    const td = {
        textAlign: "right",
        width:"150px"
    }
    let d = new Date(Date.parse(props.value.created));
    let f = new
    
}


export default Item